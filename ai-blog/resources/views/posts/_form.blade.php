@csrf
<div class="form-field"><label for="title">Title</label><input id="title" name="title" type="text" value="{{ old('title', $post->title ?? '') }}" placeholder="A title worth opening" required>@error('title')<span class="field-error">{{ $message }}</span>@enderror</div>
<div class="form-field"><label for="category_id">Category</label><select id="category_id" name="category_id" required><option value="">Choose a category</option>@foreach ($categories as $category)<option value="{{ $category->id }}" @selected(old('category_id', $post->category_id ?? '') == $category->id)>{{ $category->name }}</option>@endforeach</select>@error('category_id')<span class="field-error">{{ $message }}</span>@enderror</div>
<div class="form-field"><label for="feature_image">Feature image URL <span>(optional · Picsum works well)</span></label><input id="feature_image" name="feature_image" type="url" value="{{ old('feature_image', $post->feature_image ?? '') }}" placeholder="https://picsum.photos/seed/my-story/1200/800">@error('feature_image')<span class="field-error">{{ $message }}</span>@enderror</div>
<div class="form-field"><label for="body">Story</label><textarea id="body" name="body" rows="14" placeholder="What do you want to say?" required>{{ old('body', $post->body ?? '') }}</textarea>@error('body')<span class="field-error">{{ $message }}</span>@enderror</div>
<div class="form-actions"><a href="{{ url()->previous() }}" class="button button-secondary">Cancel</a><button type="submit" class="button">{{ $submitLabel }}</button></div>
